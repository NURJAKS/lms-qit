# -*- coding: utf-8 -*-
"""
Расширенная теория по темам курсов Python (1) и Web (2) в формате Markdown.
Используется в migrate_topic_descriptions_standalone.py и в seed_data (при необходимости).
"""

DESCRIPTIONS_COURSE_1 = [
    # 1. Python дегеніміз не?
    """## Python дегеніміз не?

**Python** — интерпретацияланатын жоғары деңгейлі программалау тілі. Оны 1991 жылы **Гвидо ван Россум** (Guido van Rossum) жасады. Тіл атауы «Монти Пайтон» комедиясынан шыққан.

### Неге Python таңдау керек?

- **Қарапайым синтаксис** — оқуға және жазуға оңай, кішкене кодпен көп нәрсе жасауға болады
- **Көп мақсатты** — веб-сайттар, деректер талдау, автоматтандыру, жасанды интеллект, скриптерлер
- **Үлкен қоғамдастық** — көмек, құжаттама және кітаптар көп
- **Кроссплатформалық** — Windows, macOS, Linux-та бірдей жұмыс істейді

### Қолдану салалары

- Веб-әзірлеу (Django, Flask)
- Деректер талдау және ML (pandas, scikit-learn)
- Автоматтандыру және скриптерлер
- Жасанды интеллект және нейрон желілер

Бұл сабақта сіз видеоны қарап, Python негіздерімен танысасыз. Келесі сабақтарда айнымалылар, операторлар және бағдарламалық логиканы үйренесіз.
""",
    # 2. Айнымалылар және деректер түрлері
    """## Айнымалылар және деректер түрлері

**Айнымалы** — деректерді сақтау үшін пайдаланылатын атау. Python-да айнымалы жасау үшін **түрін жазудың қажеті жоқ**: тек атау мен мән беріледі.

### Айнымалы қалай жасалады

```python
x = 5
аты = "Айгүл"
бағасы = 19.99
```

### Негізгі деректер түрлері

| Түр | Сипаттама | Мысал |
|-----|-----------|--------|
| **int** | Бүтін сандар | `42`, `-7`, `0` |
| **float** | Нақты сандар (үтірлі) | `3.14`, `-0.5` |
| **str** | Мәтін (string) | `"Сәлем"`, `'дүние'` |
| **bool** | Логикалық (True/False) | `True`, `False` |
| **list** | Реттелген тізім | `[1, 2, 3]`, `["а", "б"]` |
| **dict** | Кілт-мән жұптары (сөздік) | `{"аты": "Бек", "жасы": 20}` |

### Түрді тексеру

`type()` функциясы айнымалының түрін қайтарады:

```python
x = 5
print(type(x))   # <class 'int'>

мәтін = "Python"
print(type(мәтін))  # <class 'str'>
```

### Жаңа айнымалыға мән беру

Бір жолда бірнеше айнымалыға тағайындауға болады:

```python
a, b = 1, 2
аты, жасы = "Нұрбол", 18
```

**None** — «мән жоқ» дегенді білдіретін арнайы мән. Айнымалы әлі толтырылмағанда қолданылады.
""",
    # 3. Операторлар
    """## Операторлар

Операторлар — мәндермен жұмыс істеу үшін қолданылатын белгілер. Python-да бірнеше топтар бар.

### Арифметикалық операторлар

| Оператор | Мағынасы | Мысал |
|----------|----------|--------|
| `+` | Қосу | `5 + 3` → 8 |
| `-` | Алу | `10 - 4` → 6 |
| `*` | Көбейту | `6 * 7` → 42 |
| `/` | Бөлу (нәтиже әрқашан float) | `10 / 4` → 2.5 |
| `//` | Бүтін бөлу | `10 // 4` → 2 |
| `%` | Қалдық | `10 % 4` → 2 |
| `**` | Дәрежелеу | `2 ** 3` → 8 |

### Салыстыру операторлары

Нәтиже әрқашан **bool** (True немесе False):

```python
5 == 5   # True
5 != 3   # True
5 > 3    # True
5 <= 5   # True
```

### Логикалық операторлар

- **and** — екеуі де шын болса шын
- **or** — кем дегенде біреуі шын болса шын
- **not** — терістеу

```python
True and False   # False
True or False    # True
not True         # False
```

### Қысқартылған тағайындау

```python
x = 10
x += 5   # x = x + 5  → 15
x -= 2   # x = 13
x *= 2   # x = 26
```

Операторларды қатар қолдануға болады; орындау реті математикадағыдай: жақша, дәреже, көбейту/бөлу, қосу/алу.
""",
    # 4. Шартты операторлар
    """## Шартты операторлар

Шартты операторлар программаның **шартқа байланысты** әр түрлі код блоктарын орындауына мүмкіндік береді.

### if, elif, else

- **if** — шарт орындалса, блок орындалады
- **elif** — алдыңғы шарт орындалмаса, келесі шартты тексеру
- **else** — ешқандай шарт орындалмаса

```python
жасы = 18

if жасы >= 18:
    print("Сіз толық жасқа толдыңыз")
elif жасы >= 14:
    print("Жастағы шектеулер бар")
else:
    print("Сіз әлі кішкенесіз")
```

### Python-да блоктар — кеңістік (indentation)

Басқа тілдерден айырмашылығы — Python **жақша емес**, **бош орындар (пробелдер)** арқылы блокты анықтайды. Әдетте 4 пробел қолданылады:

```python
if x > 0:
    print("Оң сан")
    print("Екінші жол да if-ке жатады")
print("Бұл жол әрқашан орындалады")
```

### Қатаң ережелер

- **if** жалғыз қолдануға болады
- **elif** қалағанша болуы мүмкін
- **else** бір рет, соңғыда
- Шарттан кейін **көгілдір нүкте** (`:`) қойылады

Шарттарда салыстыру және логикалық операторлар қолданылады: `==`, `!=`, `>`, `<`, `and`, `or`, `not`.
""",
    # 5. Циклдар
    """## Циклдар

Циклдар — бір әрекетті **қайталау** үшін қолданылады. Python-да екі негізгі цикл бар: **for** және **while**.

### for циклы

**for** — тізім немесе диапазон бойынша әр элемент үшін бір рет орындалады:

```python
# Тізім бойынша
тізім = ["алма", "алмұрт", "шабдалы"]
for жеміс in тізім:
    print(жеміс)
```

### range() функциясы

`range(n)` — 0-ден n-1-ге дейінгі сандар тізбегі. Циклда N рет орындау үшін ыңғайлы:

```python
for i in range(5):
    print(i)   # 0, 1, 2, 3, 4

# range(бастау, аяқ) — бастаудан аяқ-1-ге дейін
for i in range(2, 6):
    print(i)   # 2, 3, 4, 5
```

### while циклы

**while** — шарт **True** болғанша цикл қайталанады:

```python
сан = 0
while сан < 5:
    print(сан)
    сан += 1
```

### break және continue

- **break** — циклды дереу тоқтатады, шығады
- **continue** — қалған кодты өткізіп, келесі итерацияға өтеді

```python
for i in range(10):
    if i == 3:
        continue   # 3-ті өткізіп жібереді
    if i == 7:
        break      # 7-де цикл тоқтайды
    print(i)
```

Циклдарды бір-біріне қосу (ішкі циклдар) арқылы кестелер мен күрделі қайталаулар жасауға болады.
""",
    # 6. Функциялар негіздері
    """## Функциялар негіздері

**Функция** — белгілі бір әрекетті орындайтын қайта қолданылатын код блогы. Үлкен бағдарламаны шағын, түсінікті бөліктерге бөлу үшін функциялар өте маңызды.

### Неге функция керек?

- **Қайталанатын кодты азайтады** — бір логиканы бірнеше жерде көшірудің қажеті жоқ
- **Оқылуын жақсартады** — код мағыналы блоктарға бөлінеді
- **Тестілеуді жеңілдетеді** — әр функцияны бөлек тексеруге болады
- **Қолдауды жеңілдетеді** — өзгеріс тек бір жерде жасалады

### Функцияны анықтау

Python-да функция `def` кілтсөзімен жазылады:

```python
def сәлемдесу():
    print("Сәлем, оқушы!")
```

Функция орындалуы үшін оны **шақыру** керек:

```python
сәлемдесу()
```

### Параметрлер және аргументтер

- **Параметр** — функция анықтамасындағы айнымалы
- **Аргумент** — функцияны шақырғанда берілетін нақты мән

```python
def амандасу(аты):
    print(f"Сәлем, {аты}!")

амандасу("Айгүл")
```

### return операторы

Егер функция нәтиже беруі керек болса, `return` қолданылады:

```python
def қосу(a, b):
    return a + b

нәтиже = қосу(5, 7)
print(нәтиже)  # 12
```

`print()` тек экранға шығарады, ал `return` мәнді функциядан сыртқа қайтарады.

### Әдепкі (default) параметрлер

```python
def профиль(аты, қала="Алматы"):
    return f"{аты} — {қала}"

print(профиль("Нұрлан"))           # Нұрлан — Алматы
print(профиль("Нұрлан", "Астана")) # Нұрлан — Астана
```

### Жақсы тәжірибе

- Функция атауын әрекетке сай қойыңыз: `есептеу_баға()`, `тексеру_қолжетімділік()`
- Бір функция бір негізгі міндет атқарсын
- Қысқа және түсінікті болсын
- Қажет болса қысқа түсіндірме (`docstring`) жазыңыз

Функциялар — кәсіби Python кодтың негізі. Келесі қадамда осы функцияларды деректер коллекцияларымен бірге қолдану маңызды болады.
""",
    # 7. Тізімдер және кортеждер
    """## Тізімдер және кортеждер

Python-да бірнеше мәнді бірге сақтау үшін жиі **тізім** (`list`) және **кортеж** (`tuple`) қолданылады. Екеуі де реттелген коллекциялар, бірақ өзгерту мүмкіндігі әртүрлі.

### Тізім (list)

Тізім — **өзгеретін** (mutable) құрылым:

```python
пәндер = ["Математика", "Python", "Web"]
print(пәндер[0])   # Математика
```

Тізіммен жиі қолданылатын әрекеттер:

```python
сандар = [10, 20, 30]
сандар.append(40)      # соңына қосу
сандар.insert(1, 15)   # индекс бойынша енгізу
сандар.remove(20)      # мән бойынша өшіру
соңғы = сандар.pop()   # соңғы элементті алып тастау және қайтару
```

### Кортеж (tuple)

Кортеж — **өзгермейтін** (immutable) құрылым:

```python
түстер = ("қызыл", "жасыл", "көк")
print(түстер[1])  # жасыл
```

Кортежде `append` немесе `remove` сияқты өзгерту әдістері жоқ. Бұл оның қауіпсіздігін және кей жағдайда өнімділігін арттырады.

### List пен tuple айырмашылығы

| Қасиет | list | tuple |
|--------|------|-------|
| Өзгерту | Иә | Жоқ |
| Жазылуы | `[]` | `()` |
| Қолдану | Деректер жиі өзгерсе | Тұрақты деректер үшін |

### Тізімді циклмен оқу

```python
оқушылар = ["Аян", "Меруерт", "Диас"]
for аты in оқушылар:
    print(аты)
```

### Индекс және кесінді (slicing)

```python
nums = [0, 1, 2, 3, 4, 5]
print(nums[2])      # 2
print(nums[-1])     # 5
print(nums[1:4])    # [1, 2, 3]
```

### Оралымды жұмыс істеу тәсілдері

- `len(тізім)` — элемент саны
- `in` — элемент бар-жоғын тексеру
- `sorted(тізім)` — сұрыпталған жаңа тізім
- `sum(сандар)` — сандар қосындысы

Тізімдер мен кортеждерді дұрыс пайдалану — деректерді тиімді өңдеудің негізі. Келесі сабақтарда оларды сөздіктермен, функциялармен және файлдармен бірге қолданасыз.
""",
]

DESCRIPTIONS_COURSE_2 = [
    # 1. HTML дегеніміз не?
    """## HTML дегеніміз не?

**HTML** (HyperText Markup Language, «гипермәтінді белгілеу тілі») — веб-беттердің **құрылымын** сипаттайтын **разметка (белгілеу) тілі**. Бұл **бағдарламалау тілі емес**: ол нұсқау бермейді, тек мазмұнды бөліктерге бөледі (тақырып, параграф, сілтеме, сурет және т.б.).

### Неге HTML керек?

- **Браузер** (Chrome, Firefox, Safari…) HTML кодын оқып, пайдаланушыға парақты **көрсетеді**.
- **CSS** — сыртқы түрі: түстер, шрифттер, орналасу.
- **JavaScript** — интерактивтілік: түймелер, тексерулер, динамикалық өзгерістер.

Келесі тақырыптарда сіз нақты **тегтер** (`<h1>`, `<p>`, `<a>`) және құжат құрылымын үйренесіз. Бұл сабақта мақсат — HTML-дің рөлін және веб-технологиялар стегін түсіну.

### Қысқа шолу

| Технология | Не істейді? |
|------------|-------------|
| HTML | Мазмұнның құрылымы мен мағынасы |
| CSS | Көрініс пен орналасу |
| JavaScript | Мінез-құлық және логика |

Файлдар әдетте `.html` кеңейтімімен сақталады; құжат `<!DOCTYPE html>` және `<html>` тегтерінен басталады (оларды келесі сабақта толық қарастырамыз).
""",
    # 2. Формалар
    """## HTML формалар

HTML **формалар** пайдаланушыдан деректер жинау үшін қолданылады: логин, тіркелу, пікір, сұраныс және т.б.

### <form> тегі

Форманың негізі — `<form>` тегі. Маңызды атрибуттар:

- **action** — деректер жіберілетін сервердің URL-і
- **method** — **GET** (параметрлер URL-ге қосылады) немесе **POST** (құпия деректер үшін, мысалы құпия сөз)

```html
<form action="/save" method="POST">
  <!-- форма элементтері -->
</form>
```

### Негізгі элементтер

| Элемент | Қызметі |
|---------|---------|
| `<input type="text">` | Мәтін енгізу өрісі |
| `<input type="email">` | Электрондық пошта (валидациямен) |
| `<input type="password">` | Құпия сөз (символдар жасырылады) |
| `<input type="number">` | Сан енгізу |
| `<textarea>` | Көп жолды мәтін |
| `<select>` және `<option>` | Тізімнен таңдау (dropdown) |
| `<button type="submit">` | Форманы жіберу түймесі |

### name атрибуты

Әр элементке **name** қойылуы керек — сервер деректерді осы атаулар арқылы алады:

```html
<input type="text" name="аты" placeholder="Атыңыз">
<input type="email" name="email" required>
<button type="submit">Жіберу</button>
```

**label** тегі — өріске анықтама беру және басу арқылы фокус беру үшін. **placeholder** — өрісте көрсетілетін үлгі мәтін.
""",
    # 3. Семантикалық HTML
    """## Семантикалық HTML

**Семантикалық HTML** — беттің бөліктерінің **мағынасын** анықтайтын тегтер. `<div>` орнына мағыналы тегтер қолдану кодты түсінікті етеді және қолдау технологияларына (экран оқу құралдары, іздеу жүйелери) көмектеседі.

### Негізгі семантикалық тегтер

| Тег | Қызметі |
|-----|---------|
| `<header>` | Беттің немесе бөлімнің басы (логотип, навигация) |
| `<nav>` | Навигациялық сілтемелер |
| `<main>` | Беттің негізгі мазмұны (**бір рет** қолданылады) |
| `<article>` | Өзіндік мазмұн (мақала, пост, карточка) |
| `<section>` | Тақырыптық топтасқан бөлім |
| `<aside>` | Қосымша ақпарат (жақындағы посттар, реклама) |
| `<footer>` | Төменгі бөлім (сілтемелер, авторлық құқық) |

### Мысал құрылым

```html
<body>
  <header>
    <nav>...</nav>
  </header>
  <main>
    <article>
      <h1>Мақала тақырыбы</h1>
      <section>...</section>
    </article>
    <aside>Қосымша ақпарат</aside>
  </main>
  <footer>© 2025</footer>
</body>
```

### Артықшылықтары

- **Экран оқу құралдары** — қолданушы бет құрылымын жақсы түсінеді
- **Іздеу жүйелері** — мазмұнды индекстеу оңайырақ
- **Код оқуға оңай** — әр бөлімнің рөлі анық
""",
    # 4. CSS селекторлары
    """## CSS селекторлары

CSS **селекторлары** қандай элементтерге стиль қолданылатынын анықтайды. Дұрыс селекторлар жазу — тиімді және қысқа стильдердің кілті.

### Негізгі селекторлар

| Селектор | Мысал | Сипаттама |
|----------|--------|-----------|
| Элемент | `p`, `div`, `h1` | Тег аты бойынша |
| Класс | `.container`, `.btn` | `class="container"` |
| ID | `#header` | `id="header"` (бір элементке) |

```css
p { color: blue; }
.title { font-size: 24px; }
#main { max-width: 800px; }
```

### Комбинаторлар

- **div p** — div ішіндегі **барлық** p (ұрпақтар)
- **div > p** — div-тің **тікелей бала** p элементтері
- **h1 + p** — h1-ден кейінгі **бірінші** қатардағы p

### Псевдокласс және псевдоэлементтер

**Псевдокласс** — элементтің күйі:

- `:hover` — тінтуір үстінде
- `:focus` — фокус алғанда (өріс таңдалғанда)
- `:first-child`, `:last-child` — бірінші/соңғы бала

**Псевдоэлементтер** — виртуалды бөліктер:

- `::before`, `::after` — элементтің алдына/артына мазмұн қосу
- `::first-line` — бірінші жолды стильдеу

```css
a:hover { color: red; }
p::first-line { font-weight: bold; }
```
""",
    # 5. Flexbox
    """## Flexbox

**Flexbox** (Flexible Box) — элементтерді **икемді** орналастыру үшін CSS модулі. Бір ось (көлденең немесе тік) бойынша элементтерді оңай туралауға болады.

### Негізгі қасиеттер

Контейнерге **display: flex** беріледі. Содан кейін балалар **flex-элементтер** болады:

```css
.container {
  display: flex;
  flex-direction: row;    /* row | column | row-reverse | column-reverse */
  justify-content: center; /* негізгі ось бойынша: start | center | end | space-between */
  align-items: center;    /* көлденең ось: stretch | center | start | end */
  flex-wrap: wrap;        /* жолдарға ауысу: nowrap | wrap */
  gap: 1rem;              /* элементтер арасындағы қашықтық */
}
```

### Бала элементтерде

- **flex-grow** — бос орынды бөлу (ұлғайту)
- **flex-shrink** — кішірейтиру мүмкіндігі
- **flex-basis** — бастапқы өлшем

```css
.item {
  flex-grow: 1;
  flex-shrink: 0;
}
```

### Қысқартылған форма

`flex: 1` — көбінесе **flex-grow: 1; flex-shrink: 1; flex-basis: 0** дегенді білдіреді (элемент бос орынды тең бөледі).

Flexbox навигация, карточкалар, футер және ортаға туралау үшін өте ыңғайлы.
""",
    # 6. Responsive дизайн
    """## Responsive дизайн

**Responsive дизайн** — бет **барлық құрылғыларда** (компьютер, планшет, смартфон) оқылатын және ыңғайлы болатындай етіп жасалуы. Экран еніне қарай макет және стильдер өзгереді.

### Медиа-сұраныстар (Media Queries)

Экран еніне немесе биіктігіне байланысты CSS ережелерін қолдану:

```css
/* Экран ені 768px-тен кіші болса */
@media (max-width: 768px) {
  .sidebar { display: none; }
  .content { width: 100%; }
}

/* Экран ені 1024px-тен үлкен болса */
@media (min-width: 1024px) {
  .container { max-width: 1200px; }
}
```

### Viewport meta тегі

Мобильді браузерге бетті дұрыс масштабтау үшін `<head>`-та қойылады:

```html
<meta name="viewport" content="width=device-width, initial-scale=1">
```

### Ыңғайлы бірліктер

- **%** — ана элементке қатысты пайыз
- **vw, vh** — viewport ені/биіктігінің пайызы
- **rem** — түбір элементтің `font-size`-ына қатысты (әдетте 1rem = 16px)
- **min-width, max-width** — элементтің ең кіші/үлкен ені

### Mobile-first тәсілі

Алдымен **кіші экран** (мобильді) үшін стильдер жазылады, кейін `min-width` медиа-сұраныстарымен үлкен экрандар үшін қосымша стильдер қосылады. Осы тәсіл үлгісі көп қолданылады.
""",
    # 7. JavaScript айнымалылары
    """## JavaScript айнымалылары

JavaScript-та айнымалы жасау үшін **let**, **const** немесе ескі **var** қолданылады. Қазіргі жақсы тәжірибе — **const** (өзгертілмейтін) және **let** (өзгеретін).

### let және const

```javascript
let аты = "Нұрбол";
аты = "Бекзат";  // рұқсат етілген

const жасы = 18;
// жасы = 20;  // Қате! const қайта тағайындалмайды
```

**const** объектінің **ішкі қасиеттерін** өзгертуге болады, тек бүкіл айнымалыны басқа объектке қайта тағайындауға болмайды:

```javascript
const адам = { аты: "Айгүл", жасы: 25 };
адам.жасы = 26;  // рұқсат етілген
```

### Деректер түрлері

**Примитивтер:** `number`, `string`, `boolean`, `null`, `undefined`  
**Объектілер:** `object`, `array`, `function`

```javascript
let сан = 42;
let мәтін = "Сәлем";
let логика = true;
let жоқ = null;
let анықталмаған = undefined;
```

### Блок көлемі (block scope)

**let** және **const** — блок көлемінде: жақша `{ }` ішінде жарияланған айнымалы сыртта көрінбейді. **var** — функция көлемінде (ескі тәжірибе, жаңа кодта қолданбаңыз).
""",
    # 8. DOM манипуляциясы
    """## DOM манипуляциясы

**DOM** (Document Object Model) — HTML құжатының **құрылымы** браузерде объект ретінде. JavaScript арқылы элементтерді тауып, мазмұнды өзгерту, оқиғалар қосу және жаңа элементтер қосуға болады.

### Элементтерді табу

```javascript
document.getElementById("header");           // id бойынша (бір элемент)
document.querySelector(".btn");              // CSS селектор бойынша (бірінші)
document.querySelectorAll("p");              // барлық сәйкес элементтер (тізім)
```

### Мазмұнды өзгерту

```javascript
let el = document.querySelector("#title");
el.textContent = "Жаңа тақырып";   // тек мәтін (XSS қауіпсіз)
el.innerHTML = "<b>Белді мәтін</b>"; // HTML (қауіпті контентпен сақ болыңыз)
```

### Оқиғаларды тыңдау

```javascript
let btn = document.querySelector("button");
btn.addEventListener("click", function() {
  console.log("Түйме басылды");
});
```

### Жаңа элементтер қосу

```javascript
let div = document.createElement("div");
div.textContent = "Жаңа блок";
div.classList.add("card");
document.body.appendChild(div);
```

### Кластарды басқару

- **element.classList.add("class")** — класс қосу
- **element.classList.remove("class")** — класс алу
- **element.classList.toggle("class")** — бар болса алу, жоқ болса қосу

DOM арқылы формаларды оқу, кнопкаларға әсер беру және бетті динамикалық жаңартуға болады.
""",
]
